import React from "react";

const Header = () => {
    return (
        <header className="flex justify-center items-center h-10 bg-yellow-primary fixed w-full top-0 z-50">
            <h1 className="uppercase">Site à vendre</h1>
        </header>
    );
};

export default Header;
